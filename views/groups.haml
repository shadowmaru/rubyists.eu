#groups
  %h4 Ruby User Groups in Europe
  - if @groups.empty?
    .group
      %p There are no Groups added yet.
  - else
    - for group in @groups
      .group
        %img{:src => "/images/flags/#{group.location.country_code.downcase}.png"}
        - if group.website.empty?
          %p.name= group.name
        - else
          %p.name<
            %a{:href => group.website}= group.name
        %p= group.location.city
  .pagination
    = @groups.pager.to_html('/groups', :size => 5)
  .actions
    %ul
      %li.add Add a Group
